# Kept for compatibility with workflows that expect a requirements.txt.
# The core scanner has no third-party runtime dependencies. pyproject.toml
# is authoritative for the live, benchmark, test, and development optional groups.
# Install those with pip install -e ".[live]", ".[benchmark]", ".[test]", or ".[dev]".
